# ==============================================
# Re:Action Frontend Environment Variables
# ==============================================
#
# IMPORTANT: Only variables starting with VITE_ are exposed to the client-side code
# They can be accessed via import.meta.env.VITE_VARIABLE_NAME
#
# Create a .env file for development and .env.production for production builds
# ==============================================

# ======================
# API CONFIGURATION
# ======================

# Backend API base URL
# Development: Local backend server
VITE_API_BASE_URL='http://localhost:5000/api/v1'

# Production: Deployed backend URL (uncomment and update in .env.production)
# VITE_API_BASE_URL='https://your-backend-domain.com/api/v1'

# ======================
# OAUTH CONFIGURATION
# ======================

# Google OAuth 2.0 Client ID
# Get from: https://console.cloud.google.com/apis/credentials
# Must match the domain where frontend is hosted in Google Console
VITE_GOOGLE_CLIENT_ID=''

# SPID OAuth authorization URL (if implementing SPID authentication)
# Leave empty if not using SPID
VITE_SPID_AUTH_URL=''

# ======================
# APPLICATION PATHS
# ======================

# Base path for the application (used by Vue Router)
# Development: Root path
BASE_PATH='/'

# Production: If app is hosted in a subdirectory (uncomment in .env.production)
# Example: If hosted at https://example.com/app/, set BASE_PATH='/app/'
# BASE_PATH='/EasyLibApp/'

# Frontend base URL (used for absolute URLs and redirects)
# Development:
BASE_URL='http://localhost:5173'

# Production: Your deployed frontend URL (update in .env.production)
# BASE_URL='https://your-frontend-domain.com'

# ======================
# BUILD CONFIGURATION
# ======================

# Node environment
# Automatically set by Vite, but can be overridden
# NODE_ENV='development'  # for development
# NODE_ENV='production'   # for production builds

# ======================
# EXAMPLE: .env.production
# ======================
# 
# Copy this file to .env.production and update with production values:
#
# VITE_API_BASE_URL='https://api.yourproductiondomain.com/api/v1'
# VITE_GOOGLE_CLIENT_ID='your-production-google-client-id'
# BASE_PATH='/'
# BASE_URL='https://yourproductiondomain.com'
# NODE_ENV='production'
#
# ======================

# ======================
# DEVELOPMENT NOTES
# ======================

# 1. Create .env from .env.example:
#    cp .env.example .env
#
# 2. Update VITE_API_BASE_URL to match your backend URL
#
# 3. Get Google OAuth Client ID:
#    - Go to https://console.cloud.google.com/
#    - Create or select a project
#    - Enable Google+ API
#    - Create OAuth 2.0 credentials (Web application)
#    - Add authorized redirect URIs:
#      * http://localhost:5173 (development)
#      * https://your-production-domain.com (production)
#    - Copy Client ID to VITE_GOOGLE_CLIENT_ID
#
# 4. Run development server:
#    npm run dev
#
# ======================

# ======================
# PRODUCTION DEPLOYMENT
# ======================

# 1. Create .env.production with production values
# 2. Build the application:
#    npm run build
# 3. Deploy the dist/ folder to your hosting service
# 4. Ensure CORS is configured on backend to allow your frontend domain
# 5. Update Google OAuth authorized domains in Google Console
#
# ======================
    